name: ${pom.artifactId}
main: ${pom.groupId}.HG
version: "${pom.version}"
api: ["2.0.0"]
load: POSTWORLD
authors: ["bob7l", "Spoopy", "ShaneBee", "Extollite"]
description: Nukkit hunger games plugin
prefix: "${pom.artifactId}"
softdepend:
  - ScoreboardAPI